# Инструкция по работе с Git

## Основные команды для сохранения изменений

### 1. Проверить статус
```bash
git status
```
Показывает измененные файлы и новые файлы

### 2. Добавить файлы в индекс
```bash
# Добавить все файлы
git add .

# Добавить конкретные файлы
git add имя_файла.py имя_файла2.py
```

### 3. Создать коммит
```bash
git commit -m "Описание изменений"
```
Описание должно быть кратким и понятным

### 4. Отправить на GitHub
```bash
git push
```

## Важные правила

### ❗ Никогда не добавляйте в Git:
- Файлы с ключами и паролями (*.json с ключами)
- `.env` файлы с переменными окружения
- Логи (папка logs/)
- Изображения (особенно большие)

Все эти файлы уже добавлены в `.gitignore`

### 📝 .gitignore
Этот файл содержит список файлов и папок, которые Git должен игнорировать. Если добавили новые типы секретов - добавьте их туда.

## Полезные команды

### Посмотреть историю коммитов
```bash
git log --oneline -10
```

### Отменить изменения в файле
```bash
git restore имя_файла.py
```

### Отменить коммит (если еще не pushнули)
```bash
git reset --soft HEAD~1
```

### Обновить изменения из GitHub
```bash
git pull
```

## Ежедневный рабочий процесс

1. Начали работать над задачей
2. Сделали изменения
3. Проверили статус: `git status`
4. Добавили файлы: `git add .`
5. Создали коммит: `git commit -m "feat: добавил новую функцию"`
6. Отправили на GitHub: `git push`

## Примеры хороших сообщений коммитов

- ✅ `feat: добавить OAuth аутентификацию`
- ✅ `fix: исправить ошибку с загрузкой изображений`
- ✅ `docs: обновить README`
- ✅ `refactor: оптимизировать запросы к API`

## Проверка перед push

Перед `git push` всегда проверяйте:
1. `git status` - нет ли лишних файлов
2. `git diff --cached` - что именно вы коммитите

Это поможет избежать случайной отправки секретов на GitHub!